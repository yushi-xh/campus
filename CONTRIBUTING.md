# 贡献指南

感谢你考虑为校园网自动登录工具做出贡献！

## 如何贡献

### 报告 Bug

如果你发现了 Bug，请：

1. 在 [Issues](https://github.com/yourusername/campus-auto-login/issues) 中搜索是否已有相关问题
2. 如果没有，创建一个新 Issue，并包含：
   - 清晰的标题
   - 详细的问题描述
   - 复现步骤
   - 预期行为
   - 实际行为
   - 系统环境（操作系统、Python版本等）
   - 相关日志或截图

### 提出新功能

如果你有新功能的想法：

1. 先在 Issues 中讨论你的想法
2. 说明为什么这个功能有用
3. 提供详细的功能描述
4. 等待社区反馈

### 提交代码

#### 开发环境设置

```bash
# 克隆仓库
git clone https://github.com/yourusername/campus-auto-login.git
cd campus-auto-login

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 安装开发依赖
pip install pylint black pytest
```

#### 代码规范

1. **遵循 PEP 8 风格指南**
   ```bash
   # 使用 black 格式化代码
   black *.py
   
   # 使用 pylint 检查代码
   pylint *.py
   ```

2. **注释和文档**
   - 为所有函数添加文档字符串
   - 复杂逻辑添加行内注释
   - 使用中英文双语注释（可选）

3. **提交信息**
   - 使用清晰的提交信息
   - 格式：`类型: 简短描述`
   - 类型：`feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`
   
   示例：
   ```
   feat: 添加主题切换功能
   fix: 修复网络检测间隔错误
   docs: 更新README安装说明
   ```

#### Pull Request 流程

1. **Fork 项目**
   - 点击右上角的 Fork 按钮

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   ```

3. **进行修改**
   - 编写代码
   - 添加测试（如适用）
   - 更新文档

4. **提交更改**
   ```bash
   git add .
   git commit -m "feat: 添加新功能"
   ```

5. **推送到你的仓库**
   ```bash
   git push origin feature/your-feature-name
   ```

6. **创建 Pull Request**
   - 访问原仓库
   - 点击 "New Pull Request"
   - 选择你的分支
   - 填写 PR 描述，说明：
     - 改动内容
     - 解决的问题
     - 测试情况

#### 代码审查

- 维护者会审查你的代码
- 根据反馈进行修改
- 通过后会被合并

## 开发指南

### 项目结构

```
校园网自动登录工具/
├── auto_campus_login.py          # 核心登录逻辑
├── campus_login_gui.py           # GUI界面
├── build_config_optimized.spec   # 打包配置
└── tests/                        # 测试文件（待添加）
```

### 添加新功能

1. **分析需求**
   - 确认功能价值
   - 设计实现方案

2. **编写代码**
   - 保持代码简洁
   - 遵循现有风格
   - 添加适当注释

3. **测试**
   - 本地测试功能
   - 确保不影响现有功能

4. **文档**
   - 更新 README.md
   - 添加使用说明
   - 更新 CHANGELOG

### 修复 Bug

1. **复现 Bug**
   - 确认问题存在
   - 找到问题根源

2. **修复**
   - 最小化改动
   - 避免引入新问题

3. **验证**
   - 确认 Bug 已修复
   - 测试相关功能

## 代码风格

### Python 代码

```python
# 好的示例
def login_to_campus_network(username: str, password: str) -> bool:
    """
    登录校园网
    
    Args:
        username: 用户名
        password: 密码
    
    Returns:
        bool: 登录是否成功
    """
    try:
        # 实现登录逻辑
        result = perform_login(username, password)
        return result
    except Exception as e:
        logger.error(f"登录失败: {e}")
        return False
```

### 变量命名

- 使用描述性名称
- 常量使用大写字母
- 类使用驼峰命名
- 函数和变量使用下划线分隔

```python
# 好的命名
MAX_RETRY_COUNT = 3
class NetworkMonitor:
    def check_connection_status(self):
        pass

# 避免
x = 3  # 不清楚的名称
def f():  # 不描述性的函数名
    pass
```

## 测试

### 运行测试

```bash
# 运行所有测试
pytest

# 运行特定测试
pytest tests/test_login.py

# 查看覆盖率
pytest --cov=.
```

### 编写测试

```python
import pytest
from auto_campus_login import login_to_campus_network

def test_login_success():
    """测试登录成功场景"""
    result = login_to_campus_network("testuser", "testpass")
    assert result is True

def test_login_failure():
    """测试登录失败场景"""
    result = login_to_campus_network("", "")
    assert result is False
```

## 发布流程

（仅供维护者参考）

1. 更新版本号
2. 更新 CHANGELOG
3. 创建 Git tag
4. 推送到远程
5. 创建 GitHub Release
6. 上传打包文件

## 问题和帮助

如有任何问题，可以：

- 提交 Issue
- 发送邮件到 your.email@example.com
- 在 Discussions 中讨论

## 行为准则

请遵守以下准则：

- 尊重他人
- 接受建设性批评
- 关注项目的最佳利益
- 友善对待社区成员

## 许可证

贡献的代码将采用项目相同的 MIT 许可证。

---

感谢你的贡献！🎉
